MEMORY_EXTRACTION_PROMPT = """请从以下对话中提取值得长期记忆的信息。

需要提取的信息类型：
1. **用户事实**（user_fact）：用户的个人信息、偏好、背景
2. **偏好**（preference）：用户的喜好、习惯、风格倾向
3. **决策**（decision）：用户做出的重要决定或选择
4. **工具结果**（tool_result）：工具执行返回的重要结果

请按以下 JSON 格式输出，如果没有值得记忆的信息则返回空数组：
```json
[
  {{
    "type": "user_fact|preference|decision|tool_result",
    "content": "记忆内容摘要（简洁、完整的一句话）",
    "importance": 0.0-1.0 之间的浮点数（0不重要，1非常重要）
  }}
]
```

注意事项：
- 只提取有长期价值的信息，不要提取日常寒暄
- importance 根据信息的重要性和复用频率判断
- content 使用中文，第三人称表述
- 去重：如果信息与已有记忆重复，则不再提取

对话内容：
{messages}"""


MEMORY_RETRIEVAL_PROMPT = """以下是当前对话相关的历史记忆，请参考这些信息来更好地回答用户问题：

{memories}

请根据以上记忆内容，调整你的回答策略：
- 如果记忆中有用户的偏好信息，优先遵循
- 如果记忆中有之前讨论过的上下文，无需重复确认
- 如果记忆中有已执行的工具结果，直接引用
- 如果记忆不相关，忽略即可

继续当前对话："""
